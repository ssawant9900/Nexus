import { useState } from 'react';
import { Bell, Bot, Check, GitBranch, Save, ShieldCheck } from 'lucide-react';

function Toggle({ enabled, setEnabled }) { return <button onClick={() => setEnabled(!enabled)} aria-pressed={enabled} className={`flex h-5 w-9 items-center p-0.5 transition ${enabled ? 'bg-indigo-600' : 'bg-slate-300 dark:bg-slate-700'}`}><span className={`h-4 w-4 bg-white transition ${enabled ? 'translate-x-4' : ''}`}/></button>; }
function SettingRow({ title, text, enabled, setEnabled }) { return <div className="flex items-center justify-between gap-5 py-4"><div><p className="text-xs font-bold text-slate-800 dark:text-white">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{text}</p></div><Toggle enabled={enabled} setEnabled={setEnabled}/></div>; }
export default function Settings() { const [autoApply, setAutoApply] = useState(false); const [alerts, setAlerts] = useState(true); const [saved, setSaved] = useState(false); const save = () => { setSaved(true); window.setTimeout(() => setSaved(false), 1800); }; return <div className="mx-auto max-w-4xl space-y-5"><section className="flex flex-col gap-3 border-b border-slate-200 pb-5 dark:border-slate-800 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Workspace policy</p><h1 className="mt-2 text-2xl font-bold tracking-tight text-slate-950 dark:text-white">Automation controls</h1><p className="mt-1 text-sm text-slate-500">Keep a human in the loop while your pipeline policies evolve.</p></div><button onClick={save} className={saved ? 'inline-flex items-center justify-center gap-2 bg-emerald-600 px-3.5 py-2 text-xs font-bold text-white' : 'button-primary'}>{saved ? <><Check size={14}/>Saved</> : <><Save size={14}/>Save changes</>}</button></section><section className="panel dark:border-slate-800 dark:bg-[#0e141d]"><div className="panel-heading flex items-center gap-2 dark:border-slate-800"><GitBranch size={16} className="text-slate-500"/><div><p className="text-sm font-bold text-slate-900 dark:text-white">Repository connection</p><p className="text-[11px] text-slate-500">Source used for pipeline context</p></div></div><div className="grid gap-4 p-5 md:grid-cols-[1fr_auto]"><div className="border border-slate-200 bg-slate-50 px-3 py-2.5 dark:border-slate-700 dark:bg-slate-900"><p className="eyebrow">Connected repository</p><p className="mt-1 text-xs font-semibold text-slate-800 dark:text-slate-100">github.com/acme/api-service</p></div><button className="button-secondary">Manage connection</button></div></section><section className="panel dark:border-slate-800 dark:bg-[#0e141d]"><div className="panel-heading flex items-center gap-2 dark:border-slate-800"><Bot size={16} className="text-violet-600"/><div><p className="text-sm font-bold text-slate-900 dark:text-white">Recommendation policy</p><p className="text-[11px] text-slate-500">Define when the AI can take action</p></div></div><div className="divide-y divide-slate-100 px-5 dark:divide-slate-800"><SettingRow title="Queue safe patches automatically" text="Creates a proposed commit after a recommendation passes policy checks." enabled={autoApply} setEnabled={setAutoApply}/><SettingRow title="Require review for infrastructure files" text="Always keep a reviewer in control for Docker, workflow, and deployment changes." enabled={true} setEnabled={() => {}}/></div></section><section className="panel dark:border-slate-800 dark:bg-[#0e141d]"><div className="panel-heading flex items-center gap-2 dark:border-slate-800"><Bell size={16} className="text-amber-600"/><div><p className="text-sm font-bold text-slate-900 dark:text-white">Notification routing</p><p className="text-[11px] text-slate-500">Choose what needs an immediate response</p></div></div><div className="divide-y divide-slate-100 px-5 dark:divide-slate-800"><SettingRow title="Notify on failed production runs" text="Alert the workspace when a production deployment does not complete." enabled={alerts} setEnabled={setAlerts}/><div className="flex items-center gap-2 py-4 text-xs text-emerald-600"><ShieldCheck size={14}/>No notification provider connected in this frontend demo.</div></div></section></div>; }
